name: 'CodeQL: Init'
description: 'Setup the CodeQL tracer'
author: 'GitHub'
inputs:
  tools:
    description: URL of CodeQL tools
    required: true
  language:
    description: The language to be analysed
    required: true
runs:
  using: 'node12'
  main: '../../lib/setup-tracer.js'
